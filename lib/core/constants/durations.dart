class AppDurations {
  AppDurations._();

  static const clockTick = Duration(seconds: 1);
  static const minuteTick = Duration(minutes: 1);
  static const renderDelay = Duration(milliseconds: 400);
  static const workmanagerFrequency = Duration(hours: 24);
}
